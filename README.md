# VS Code + MyPy + Pre-commit Setup

This repository demonstrates how to set up **MyPy type checking** and enforce it automatically using **pre-commit hooks** inside **VS Code** for better Python code quality.

## 🔧 Tools Used
- MyPy – static type checker for Python
- Pre-commit – automates checks before committing
- VS Code – IDE with Python support

---

## 🚀 Getting Started

### 1. Clone the repository

```bash
git clone https://github.com/yourusername/mypy-precommit-setup.git
cd mypy-precommit-setup
